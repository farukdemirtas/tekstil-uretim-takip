import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "İş Hesaplama",
};

export default function IsBitirmeHesaplamaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
