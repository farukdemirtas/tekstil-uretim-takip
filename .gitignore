node_modules
**/node_modules
**/.next
# Gizli yapılandırma — asla commit edilmez
backend/.env
frontend/.env.local

# Üretim verisi (kullanıcılar, loglar, üretim) — commit edilmez; git pull bunları silmez
backend/data/*.db
backend/data/*.sqlite
backend/data/*.bak*
backend/data/*.bak_*
# SQLite geçici dosyaları (asla commit edilmemeli)
*.db-journal
*.db-wal
*.db-shm
*.log
frontend/.env.production
